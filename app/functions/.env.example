# Copy to functions/.env to override the default Gemini model without
# touching code. GEMINI_API_KEY itself is NOT set here - it is a secret,
# configured separately via `firebase functions:secrets:set GEMINI_API_KEY`.
GEMINI_MODEL=gemini-flash-lite-latest
